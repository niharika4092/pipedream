#include<stdio.h>
int main()
{
int n,re,sum=0;
printf("Enter number to be added\n");
scanf("%d",&n);
while(n>0)
{
re=n%10;
sum=sum+re;
n=n/10;
}
printf("sum of digits of entered number is %d\n", sum);
}
